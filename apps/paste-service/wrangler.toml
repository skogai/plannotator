name = "plannotator-paste"
main = "targets/cloudflare.ts"
compatibility_date = "2024-12-01"

[[kv_namespaces]]
binding = "PASTE_KV"
# Run `wrangler kv:namespace create PASTE_KV` to get your IDs and fill them in.
id = "8477247428e24f18882d79305062d9d9"
preview_id = "252fbbae267049db92ea153b93467cf4"

[vars]
# Default values target the hosted plannotator.ai deployment.
# Self-hosters must override ALLOWED_ORIGINS to point at their own portal,
# either by editing this file or setting it via `wrangler secret put`.
ALLOWED_ORIGINS = "https://share.plannotator.ai,https://plan.skogai.se,http://localhost:3001"
